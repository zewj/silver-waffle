requests>=2.31.0
# SOCKS proxy backend for `requests`. Enables socks5://, socks5h://,
# socks4:// proxy URLs on per-account proxies. requests imports this
# lazily only when a SOCKS URL is used.
PySocks>=1.7.1
# Per-instance CPU / RAM stats and crash detection.
psutil>=5.9
# Qt bindings — modern GUI, native DPI, real animations.
PySide6>=6.6
# Optional: enables the "Sign in with Browser" / QR flow. WebView2 runtime is
# preinstalled on Windows 10/11, so this works out of the box once installed.
pywebview>=5.0
# PNG encoding for the Discord bot's window screenshots.
Pillow>=10.0
# Optional: Discord bot for remote screenshots and control. Disabled until
# you paste a token in Settings → Discord Bot…
discord.py>=2.3
